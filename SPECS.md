hi, i want you to inspect current implementation of tags in frontend and backend. Now i want so that not all tags can be created, but only the tags that exist at StackOverFlow. It can be done using StackOverflow API. Plan the possible good implementation according to SOLID an OOP, consider that source of tags can be changed, or the way it is validated can be changed later

sudo sed -i 's/^# requirepass foobared/requirepass fT\321OhIJv>/' /etc/redis/redis.conf
